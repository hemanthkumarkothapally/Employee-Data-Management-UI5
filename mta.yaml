_schema-version: "3.2"
ID: comtrailui5trail
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comtrailui5trail-destination-content
  type: com.sap.application.content
  requires:
  - name: comtrailui5trail-destination-service
    parameters:
      content-target: true
  - name: comtrailui5trail-repo-host
    parameters:
      service-key:
        name: comtrailui5trail-repo-host-key
  - name: comtrailui5trail-uaa
    parameters:
      service-key:
        name: comtrailui5trail-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comtrailui5trail_html_repo_host
          ServiceInstanceName: comtrailui5trail-html5-service
          ServiceKeyName: comtrailui5trail-repo-host-key
          sap.cloud.service: comtrailui5trail
        - Authentication: OAuth2UserTokenExchange
          Name: comtrailui5trail_uaa
          ServiceInstanceName: comtrailui5trail-xsuaa-service
          ServiceKeyName: comtrailui5trail-uaa-key
          sap.cloud.service: comtrailui5trail
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comtrailui5trail-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comtrailui5trail-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comtrailui5trail.zip
      name: comtrailui5trail
      target-path: resources/
- name: comtrailui5trail
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comtrailui5trail-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comtrailui5trail-destination-service
    service-plan: lite
- name: comtrailui5trail-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comtrailui5trail-xsuaa-service
    service-plan: application
- name: comtrailui5trail-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comtrailui5trail-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
